#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main() {
  cin.tie(0);
  ios::sync_with_stdio(false);

  // 入力
  int a, b, c;
  cin >> a >> b >> c;
  string s;
  cin >> s;

  // 計算＋出力
  cout << (a + b + c) << " " << s << endl;

  return 0;
}
