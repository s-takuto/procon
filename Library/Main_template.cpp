#include <bits/stdc++.h>

using namespace std;
using ll = long long;

const string yes = "Yes";
const string no = "No";

int main() {
  cin.tie(0);
  ios::sync_with_stdio(false);

  // 入力

  // 計算

  // 出力

  return 0;
}
